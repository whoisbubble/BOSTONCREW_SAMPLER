# BOSTONCREW SAMPLER

Desktop sampler application built with Qt/QML and C++.

## Build

Open the project in Qt Creator with Qt 6.10+ and build the CMake target:

```powershell
cmake --build build-cmake -- -j1
```
